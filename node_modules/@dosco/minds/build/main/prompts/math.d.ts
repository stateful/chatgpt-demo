import { AIService, AIMemory, AIPrompt, PromptMetadata } from '../ai/useai';
export declare class MathPrompt implements AIPrompt {
    private _metadata;
    metadata(): Readonly<PromptMetadata>;
    create(ai: AIService, mem: AIMemory, query: string): string;
}
