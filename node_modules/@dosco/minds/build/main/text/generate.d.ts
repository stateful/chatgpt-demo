import { AIService, AIMemory, AIPrompt, GenerateResponse } from './index';
/**
 * The main class for various text generation tasks
 * @export
 */
export declare class GenerateText {
    private ai;
    private mem;
    private maxSteps;
    private debug;
    constructor(ai: AIService, mem?: AIMemory);
    setMaxSteps(n: number): void;
    setDebug(b: boolean): void;
    generate(query: string, prompt: AIPrompt, sessionID?: string): Promise<GenerateResponse>;
    private _generate;
    private processSeq;
    private processAction;
}
