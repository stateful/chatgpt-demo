import { AIMemory } from './useai';
export declare class Memory implements AIMemory {
    private data;
    private limit;
    constructor(limit?: number);
    add(text: string): void;
    history(): string;
    peek(): Readonly<string[]>;
}
