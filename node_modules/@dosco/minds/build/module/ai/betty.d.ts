import { AIService, AIGenerateResponse, EmbedResponse, PromptMetadata } from '../text';
/**
 * Betty: Fake AI Service for writing tests
 * @export
 */
export declare class Betty implements AIService {
    private answers;
    private data;
    private sdata;
    private index;
    constructor(answers: string[]);
    name(): string;
    generate(prompt: string, _md?: PromptMetadata, sessionID?: string): Promise<AIGenerateResponse>;
    embed(texts: string[], sessionID?: string): Promise<EmbedResponse>;
}
